#!/bin/bash
#
# Bind-mount a copy of the specified file.
#
# Usage: bind-mount <path>
#

if [ ! -f "$1" ]; then
    echo "Not a regular file: $1"
    exit 1
fi

name="$(basename "$1")"
copy="/tmp/$name"

cp "$1" "$copy"
sudo mount -o bind "$copy" "$1"
