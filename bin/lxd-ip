#!/bin/bash
#
# Return the IP address of a LXD container.
#
# Usage: lxd-ip <name>


if [ -z "$1" ]; then
    echo "Missing container name"
    exit 1
fi

lxc list "$1" --columns=4 | awk '/eth0/ { print $2; }'
