#!/bin/bash
#
# Return the IP address of a LXD container.
#
# Usage: lxd-ip <name>

name="$1"

if [ -z "$name" ]; then
    echo "Missing container name"
    exit 1
fi

lxc list "$name" --columns=n4 | awk '$2 == "'"$name"'" && /eth0/ { print $4; }'
